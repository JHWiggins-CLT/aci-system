# Pattern: {short name — e.g. "Throughput dip after new-hire cohort"}

<!--
  A pattern is the generic causal shape shared by 3+ closed investigations.
  Fill every section. Delete these comments. Cite real investigation and
  A3/Kaizen paths — a pattern with no historical instances is not a pattern.
-->

## Signal shape

{What a matching signal looks like: which metric, which family, how far off
baseline, for how long. Include the secondary tells that usually accompany it
(co-moving metrics, shift concentration, etc.).}

## Typical co-occurring events

- {event_type, typical lead time relative to the signal — e.g. "training event 1-3 weeks prior"}

## Investigation steps (run these first when the signal matches)

1. `{calc invocation with {facility}/{signal_date} placeholders}`
2. `{calc invocation}`
3. {non-calc check, e.g. "compare trainer-to-trainee ratio against SOP"}

## Floor questions when this pattern is suspected

- {question}
- {question}

## Expected resolution timeline

{e.g. "2-4 weeks. If the signal persists past 4 weeks, escalate."}

## Countermeasures that have worked

- {date} {facility}: {action}. Result: {measured outcome, cited}.
  See {a3s/closed/... or kaizens/... path}

## Countermeasures that didn't work

- {date} {facility}: {action}. Result: {what failed, cited}.
  See {investigation or artifact path}

## Historical instances

- {investigations/YYYY-Qn/{date}_{facility}_{signal}.md}
- {investigations/YYYY-Qn/{date}_{facility}_{signal}.md}
- {investigations/YYYY-Qn/{date}_{facility}_{signal}.md}
